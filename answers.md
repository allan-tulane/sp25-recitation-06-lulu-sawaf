# CMPS 2200 Recitation 06
## Answers

**Name:** Lulu Sawaf
**Name:**_________________________


Place all written answers from `recitation-07.md` here for easier grading.



- **2)**
  W(n) = W(n-1) + W(n-2) + O(1) = O(26n)
- **3)**
  S(n) = S(n-1) + O(1) = )(n)
- **4)**
  Since Fibonacci is recursive, when the algorithm incorporates the counts list, it functions very redundantly. Fibonacci in its most efficient form should only have to calculate each number once and then recall that number in its next calculation (as opposed to recalculating the lower numbers repeatedly), so this algorithm is inefficient.
- **6)**
  It will be called for each value i only once. The work and span are both O(n).
- **8)**
  The maximum number of times it will read Fsubi for any value i is 2. The work and span are both O(n)
